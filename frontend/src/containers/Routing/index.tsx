import React from 'react';
import { Switch } from 'react-router';

const Routing: React.FC = () => {
    return (
        <Switch>
            <div />
        </Switch>
    );
};

export default Routing;
